1- bootstrapservers
2- serializer
3- partitioner
4- acks (0,1,all) 
	0 = send and forget, 
	1 = producer will wait for response form leader only(record metadata)
	all = producer will wait for response from all the replicas
5- retries (default is 0)
6- max.in.flight.requests.per.connection (how many messages can be sent by producer in 
	asynchronously,unacknowledged)